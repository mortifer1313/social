import SectionHeader from '../SectionHeader';

export default function SectionHeaderExample() {
  return (
    <SectionHeader
      icon="📁"
      title="Project Structure Overview"
      description="Visual directory tree with explanations and file organization breakdown"
    />
  );
}
